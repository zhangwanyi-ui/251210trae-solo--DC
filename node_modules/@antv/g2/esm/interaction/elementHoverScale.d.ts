import { DisplayObject } from '@antv/g';
/**
 * Scale up elements on hover.
 */
export declare function elementHoverScale(root: DisplayObject, { elements: elementsof, datum, groupKey, scaleFactor, scaleOrigin, shadow, shadowColor, shadowBlur, shadowOffsetX, shadowOffsetY, zIndex, delay, emitter, state, }: Record<string, any>): () => void;
export declare function ElementHoverScale({ delay, createGroup, scale: scaleFactorParam, scaleOrigin, shadow, shadowColor, shadowBlur, shadowOffsetX, shadowOffsetY, zIndex, ...rest }: {
    [x: string]: any;
    delay: any;
    createGroup: any;
    scale: any;
    scaleOrigin: any;
    shadow: any;
    shadowColor: any;
    shadowBlur: any;
    shadowOffsetX: any;
    shadowOffsetY: any;
    zIndex: any;
}): (context: any, _contexts: any, emitter: any) => () => void;
export declare namespace ElementHoverScale {
    var props: {
        reapplyWhenUpdate: boolean;
    };
}
